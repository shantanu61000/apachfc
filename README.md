# apachfc
to host the server locally:

install npm and node on your computer

open bash terminal in the project folder and run the following command:
npm install

after installing all the dependencies run -
npm start
